services:
  - type: web
    name: joy-attendance-app
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app.py --server.port 8501 --server.address 0.0.0.0  # ← CHANGE THIS
    envVars:
      - key: SUPABASE_URL
        value: https://qjifyweayliqjvrxxxim.supabase.co
      - key: SUPABASE_KEY
        value: YOUR_NEW_KEY_HERE
      # ... other environment variables
